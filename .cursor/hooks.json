{
  "version": 1,
  "hooks": {
    "afterFileEdit": [
      {
        "command": "node -c \"${file}\" 2>&1 || echo Syntax check skipped",
        "description": "Validasi syntax JavaScript setelah file di-edit"
      },
      {
        "command": "node .cursor/validate.js \"${file}\" 2>&1 || true",
        "description": "Validasi CommonJS dan deteksi hardcoded credentials"
      },
      {
        "command": "echo ✅ File ${file} berhasil disimpan",
        "description": "Konfirmasi setelah file disimpan"
      }
    ],
    "beforeSubmitPrompt": [
      {
        "command": "node -c \"${file}\" 2>&1 || echo Syntax validation skipped",
        "description": "Check syntax JavaScript sebelum AI memproses"
      },
      {
        "command": "node .cursor/validate.js \"${file}\" 2>&1 || true",
        "description": "Validasi code standards sebelum AI memproses"
      },
      {
        "command": "node .cursor/debug.js \"${file}\" 2>&1 || true",
        "description": "Debug check - deteksi common bugs dan potential issues"
      }
    ],
    "beforeReadFile": [
      {
        "command": "node .cursor/security-check.js \"${file}\" read 2>&1 || true",
        "description": "Security check - peringatan jika membaca file sensitif"
      }
    ]
  }
}
